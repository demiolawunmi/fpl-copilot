diff --git a/airsenal/scripts/fill_fixture_table.py b/airsenal/scripts/fill_fixture_table.py
index 1937aa7..0641c5b 100644
--- a/airsenal/scripts/fill_fixture_table.py
+++ b/airsenal/scripts/fill_fixture_table.py
@@ -9,7 +9,7 @@ from sqlalchemy.orm.session import Session
 
 from airsenal.framework.data_fetcher import FPLDataFetcher
 from airsenal.framework.mappings import alternative_team_names
-from airsenal.framework.schema import Fixture, session, session_scope
+from airsenal.framework.schema import Fixture, Team, session, session_scope
 from airsenal.framework.season import CURRENT_SEASON, sort_seasons
 from airsenal.framework.utils import find_fixture, get_past_seasons
 
@@ -69,26 +69,22 @@ def fill_fixtures_from_api(season: str, dbsession: Session = session) -> None:
 
         home_id = fixture["team_h"]
         away_id = fixture["team_a"]
-        found_home = False
-        found_away = False
-        for k, v in alternative_team_names.items():
-            if str(home_id) in v:
-                f.home_team = k
-                found_home = True
-            elif str(away_id) in v:
-                f.away_team = k
-                found_away = True
-            if found_home and found_away:
-                break
+        # Dynamic, season-aware FPL-id → team-code mapping from the Team
+        # table. FPL renumbers team ids across seasons, so the hardcoded
+        # alternative_team_names id lists silently mis-assign clubs after a
+        # rollover (e.g. 2026/27: id 7 = Coventry, not Chelsea).
+        id_to_code = {
+            str(t.team_id): t.name
+            for t in dbsession.query(Team).filter_by(season=season).all()
+        }
+        f.home_team = id_to_code.get(str(home_id))
+        f.away_team = id_to_code.get(str(away_id))
 
-        if not found_home and found_away:
-            msg = f"Can't find team(s) with id(s): {home_id}, {away_id}."
-            raise ValueError(msg)
-        if not found_home:
-            msg = f"Can't find team(s) with id(s): {home_id}"
-            raise ValueError(msg)
-        if not found_away:
-            msg = f"Can't find team(s) with id(s): {away_id}"
+        if not f.home_team or not f.away_team:
+            missing = [
+                i for i, code in ((home_id, f.home_team), (away_id, f.away_team)) if not code
+            ]
+            msg = f"Can't find team(s) with id(s): {', '.join(str(m) for m in missing)} for {season}."
             raise ValueError(msg)
         if not update:
             dbsession.add(f)
diff --git a/airsenal/scripts/update_db.py b/airsenal/scripts/update_db.py
index 6cbbc5a..a4330f0 100644
--- a/airsenal/scripts/update_db.py
+++ b/airsenal/scripts/update_db.py
@@ -137,6 +137,9 @@ def add_players_to_db(
         p.name = name
         if not update:
             dbsession.add(p)
+            # Assign the autoincrement player_id before adding mappings,
+            # otherwise PlayerMapping.player_id is NULL (NOT NULL violation).
+            dbsession.flush()
             add_mappings(p, dbsession=dbsession)
 
     dbsession.commit()
